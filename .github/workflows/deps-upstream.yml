name: deps upstream

on:
  schedule:
    - cron: '17 6 * * 1'   # Mondays 06:17 UTC
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  check:
    uses: Asmod4n/git-submodule-notifier/.github/workflows/deps-upstream.yml@a528af65c1b33828e7779a3d6f1c80e820cebcd5
    with:
      notify: '@asmod4n'
