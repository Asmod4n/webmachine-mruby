#include "flow.hpp"

// This translation unit exists so the table's compile-time proofs
// (order, edge validity, termination, reachability) run on every
// build. The interpreter arrives with the konst tiers.
